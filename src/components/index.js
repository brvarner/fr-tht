// You'll find an index file in the root of the helpers, containers, and components folders.
// These files allow us access to every export we need with a neater import syntax.

// Each of these components is built with Styled Components, which allows us to use
// different subcomponents for each one, instead of creating new components for every function.
export { default as Form } from "./form";
export { default as Dropdown } from "./dropdown";
export { default as Spacer } from "./spacer";
export { default as Footer } from "./footer";
export { default as Hero } from "./hero";
